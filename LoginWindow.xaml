<Window x:Class="ZooTicket.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ZooTicket"
        mc:Ignorable="d"
        Title="LoginWindow" Height="450" Width="800">
    <Grid Margin="20">
        <StackPanel>
            <TextBlock Text="Вход в систему" FontSize="20" FontWeight="Bold" 
                      HorizontalAlignment="Center" Margin="0,0,0,30"/>

            <TextBlock Text="Логин:" FontWeight="Bold" Margin="0,0,0,5"/>
            <TextBox x:Name="UsernameTextBox" Height="30" FontSize="14" 
                    Text="кассир"/>

            <TextBlock Text="Пароль:" FontWeight="Bold" Margin="0,10,0,5"/>
            <PasswordBox x:Name="PasswordBox" Height="30" FontSize="14"/>

            <Button Content="Войти" Click="LoginButton_Click" 
                    Background="#2E86AB" Foreground="White" 
                    FontSize="14" Height="35" Margin="0,20,0,0"/>

            <TextBlock x:Name="ErrorText" Foreground="Red" 
                      Margin="0,10" TextWrapping="Wrap"/>
        </StackPanel>
    </Grid>
</Window>
